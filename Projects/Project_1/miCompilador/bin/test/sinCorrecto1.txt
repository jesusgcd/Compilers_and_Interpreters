sumatotal = (c * 4) - (d + 5) / (z - 2);
producto = (x + y) * (x - y) / (x * y);
valormax = (x + y - z) / (a + b);
factorial = (numero * numero) / (res - resul);
tempcelsius = (tempfahre - 32) * (5 / 9);
distancia = (velocidad * tiempo) + aceleracion / 2;
atriangulo = (base * altura) / 2;
hipotenusa = (cateto * 2 + cate * 2) * 5;
media = (val + valor + v) / 3;